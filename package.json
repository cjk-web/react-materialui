{
  "name": "react-material",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^1.5.0",
    "@material-ui/icons": "^2.0.2",
    "json-server": "^0.14.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "devstart": "npm run dev",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "react-start": "react-scripts start",
    "run-server": "json-server --watch store.json --port 3004",
    "dev": "(start npm run react-start && start npm run run-server) || (start npm run react-start & start npm run run-server)"
  },
  "devDependencies": {
    "json-server": "0.14.0"
  }
}
